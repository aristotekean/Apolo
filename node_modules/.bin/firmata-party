#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../firmata-party/bin/firmata-party.js" "$@"
  ret=$?
else 
  node  "$basedir/../firmata-party/bin/firmata-party.js" "$@"
  ret=$?
fi
exit $ret
